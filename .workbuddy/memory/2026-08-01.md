# 2026-08-01 工作日志

## BMS-YOLO IEEE Access 论文架构图补充

- 阅读了 `IEEE ACCESS_latex/access_final.tex` 最终版源文件，发现 Fig. 1 的 `\includegraphics` 被注释掉，导致 `access_final.pdf` 缺少整体架构图。
- 使用 matplotlib 重新绘制了高清矢量版 BMS-YOLO 整体架构图，保存为：
  - `IEEE ACCESS_latex/figure1_architecture.pdf`（40 KB，矢量）
  - `IEEE ACCESS_latex/figure1_architecture.png`（373 KB，预览）
- 图表包含：Input 640×640、Stem+FDDE、Backbone（BMSC2f+MorphSparseMoE，P2-P5）、Neck（LightSPPFCSPC+ECA、FPN/PAN）、Decoupled Head（P3/P4/P5）、Post-processing（Confidence Calibration → NMS → Topology Fusion）。
- 更新了 `access_final.tex`，取消注释并改为 `\includegraphics[width=\linewidth]{figure1_architecture.pdf}`，删除 TODO 注释。
- 更新了 `access_final.tex`，取消注释并改为 `\includegraphics[width=\linewidth]{figure1_architecture.pdf}`，删除 TODO 注释。

## 编译最终 PDF（补充）

- 环境实际已安装 **TeX Live 2026**，路径 `/d/texlive/2026/bin/windows/`（之前用 `which` 漏检，用 `command -v` 才发现）。
- 编译时发现并修复一个真实错误：`access_final.tex` 第 35 行 `\author{...}` 缺少右花括号 `}`，导致 `\address` 被吞入 `\author` 参数而编译失败。已补上 `}`。
- 完整编译流程（在 `IEEE ACCESS_latex/` 目录，PATH 加入 tex 二进制目录）：
  1. `pdflatex -interaction=nonstopmode access_final.tex`
  2. `bibtex access_final`
  3. `pdflatex` ×2（处理交叉引用与参考文献）
- 结果：生成 `access_final.pdf`（13 页，85 MB），Fig. 1 架构图成功嵌入第 5 页（`[5 <./figure1_architecture.pdf>]`），无任何图片缺失错误。其余警告（字体 TS1/times、hyperref 书签锚点）均非致命。
- 注意：`access_final.pdf` 体积大（约 85 MB）是因为 qualitative_fig_*.pdf 等大图被嵌入，属正常。

## BMS-YOLO 投稿稿多视角审阅

- 调用 `academic-research-skills/academic-paper-reviewer` v1.10.0 框架，完整阅读 `access_final.pdf`（13 页）并逐页渲染核查所有图。
- 关键发现：Fig 2 caption 写 "only 2.3M parameters"，与全文其它位置（Abstract/Table 2/Table 6）的 3.8M 严重矛盾；源头在 `access_final.tex` 第 292 行。
- 所有 Fig 1–8 均正常嵌入 PDF，无图缺失；Fig 1 架构图清晰，qualitative 图对比清楚。
- 其它主要问题：batch size 不一致（baseline 16 vs proposed 48）却称 identical conditions；Fig 2 中 RT-DETR-l 的 "FPS N/A" 标签冗余；top-k / topology fusion 阈值 / 两阶段 epoch 划分缺少 ablation 说明。
- 已生成结构化审阅报告：`BMS-YOLO_Access_Paper_Review_Report.md`。

## 按审阅报告修改并 git 保存（补充）

- 建立了 git 工作流：先 `git commit` 原始版本为基线，再修改。
  - 仓库已有（master 分支，身份 liuqi / 3103561180@qq.com），之前无任何 commit。
  - 新增 `IEEE ACCESS_latex/.gitignore`，排除构建中间产物（*.aux/log/out/blg）、审阅脚手架（_review_pages/、access_final_text.txt）、旧草稿 PDF（access.pdf/access_new.pdf）、JPG 预览。
  - 基线 commit：`d773bde` "baseline: original BMS-YOLO IEEE Access submission (pre review-fixes)"，纳入 access_final.tex/.pdf、所有图源、references、cls 等 71 个文件。
- 已应用的修改（commit `f20083f`）：
  1. **Critical**：Fig.2 caption `2.3M` → `3.8M`（第 292 行），与全文一致。
  2. **Major**：比较段 "identical conditions" → "matched conditions/settings"（第 259、263 行），避免与 16 vs 48 batch 矛盾。
  3. **Major**：Implementation Details 末尾新增一句，明确 baseline 与 proposed 仅 batch size（16 vs 48）不同。
  - 重新编译通过，13 页，最终 PDF 已更新。
## Fig.2 重画与 FPS 全文字同步（补充）

- 重画了 Fig.2：`generate_fps_params.py` 用 matplotlib 生成 `fig6_fps_params.pdf/.png`。
  - 数据直接使用 Table 2 的 Params/mAP50 和用户实测 FPS（46.4/38.9/45.2/44.3/26.9/32.3）。
  - 配色复用 Fig.1 色族（blue/orange/purple/green/teal/gray）。
  - 6 个模型同等样式（无单独高亮 Ours），点大小编码 mAP50，X 轴对数 Params，去掉冗余 "FPS N/A"。
- 用户选择"同步更新 Table 2"后，进一步发现 Table 6、Abstract、Conclusion、正文仍用旧 FPS，于是全部同步：
  - Table 2 FPS：YOLOv8n 47.4→46.4，YOLOv9t --→38.9，YOLOv10n 46.4→45.2，YOLOv11n --→44.3，RT-DETR-l --→26.9，BMS-YOLO-n 31.8→32.3。
  - Table 6 FPS：YOLOv8n 47.4→46.4，YOLOv10n 46.4→45.2，BMS-YOLO-n 31.8→32.3。
  - 文字：Abstract/Conclusion/讨论段中所有 "31.8 FPS" → "32.3 FPS"；"reduction of 15.6 FPS" → "14.1 FPS"；"approximately 33%" → "approximately 30%".
  - 验证：PDF 中 31.8/47.4/15.6 全部归零，新值均出现，33% 归零、30% 出现 1 次。
- Git commit 记录：
  - `6c820bc` "fix: regenerate Fig.2 and sync Table 2 FPS to measured benchmarks"
  - `dc4b1c8` "fix: sync remaining FPS values across paper"

## 审阅报告剩余项处理（用户给真实值后）

- 用户提供了真实值：MoE top-k=2（4专家激活2）、Topology Fusion 阈值（裂纹类IoU 0.12/距离0.08×D与0.65×max/方向一致；非裂纹0.35；融合65%包络+35%加权平均）、两阶段训练（预训练300 epoch λ=0.02 + 微调100 epoch λ=0.005，LR 0.001 cosine，共400）、architecture–loss 维持、Pothole −0.9 归因。
- **重要发现**：核对当前 tex 后发现，MoE top-k（L165）、Topology Fusion 全部阈值（L215）、Pothole 随机波动表述（L447）其实**早已在正文中**（之前审阅报告误判为缺失）。用户确认这三项保持现状，不插入以免重复。
- **实际修改**：仅第3项（两阶段 epoch）有真实新增价值，但插入会与 Implementation Details「All models trained for 300 epochs / LR 0.01 linear」冲突。改为**替换** L256 开头句，区分基线/单阶段消融（300 epoch）与两阶段 BMS-YOLO（300预训练+100微调=400，微调LR 0.001 cosine），并引用 Table 3/Table 4。
- commit `d2e2fe1` "fix: clarify two-stage training in Implementation Details"。
- 结论：审阅报告里 Major3「缺超参说明」基本是误报；真正需补的只有两阶段 epoch/LR，已修正。

## 添加作者简介照片

- 用户提供证件照 `C:/Users/六七/Desktop/202508543036+刘奇.jpg`。
- 复制为 `IEEE ACCESS_latex/author_qi_liu.jpg`（避免中文/特殊字符导致 LaTeX 路径问题）。
- 将 `\begin{IEEEbiographynophoto}{Qi Liu}` 改为 `\begin{IEEEbiography}[{\includegraphics[width=1in,height=1.25in,clip,keepaspectratio]{author_qi_liu.jpg}}]{Qi Liu}`，并对应改结束标签为 `\end{IEEEbiography}`。
- 重新编译后照片成功显示在最后一页作者简介左侧，尺寸合适、无拉伸。
- commit `557b8c0` "feat: add author photo to biography"。

## 教训

- 之前 `academic-paper-reviewer` 报告的「Major 3 缺超参说明（top-k / topology 阈值 / 两阶段 epoch）」与正文实际不符，属误判。下次应先逐行核对 tex 再下「缺失」结论。
- 用户给出「可插入文本」时，仍需先核对正文是否已有，避免盲目插入导致重复句。
